export enum Route {
  HOME = "/",
  SIGNIN = "/sign-in",
  WIDGETS = "/widgets",
  WALLET = "/wallet",
  ACCOUNT = "/account",
  STRATEGY = "/strategy",
  CONTACT = "/contact",
  TERMS = "/terms",
  PRIVACY = "/privacy",
}
