export enum Route {
  HOME = "/",
  SIGNIN = "/sign-in",
  DASHBOARD = "/dashboard",
  WALLET = "/wallet",
  ACCOUNT = "/account",
  STRATEGY = "/strategy",
  CONTACT = "/contact",
  TERMS = "/terms",
  PRIVACY = "/privacy",
}
